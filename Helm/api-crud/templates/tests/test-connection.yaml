apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-crud.fullname" . }}-test-connection"
  labels:
    {{- include "api-crud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-crud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
