apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-processamento-ordens.fullname" . }}
  labels:
    {{- include "api-processamento-ordens.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-processamento-ordens.selectorLabels" . | nindent 4 }}
