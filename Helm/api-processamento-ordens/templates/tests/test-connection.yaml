apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-processamento-ordens.fullname" . }}-test-connection"
  labels:
    {{- include "api-processamento-ordens.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-processamento-ordens.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
