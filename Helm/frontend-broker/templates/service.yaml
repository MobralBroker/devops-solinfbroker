apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend-broker.fullname" . }}
  labels:
    {{- include "frontend-broker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frontend-broker.selectorLabels" . | nindent 4 }}
