apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontend-broker.fullname" . }}-test-connection"
  labels:
    {{- include "frontend-broker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontend-broker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
