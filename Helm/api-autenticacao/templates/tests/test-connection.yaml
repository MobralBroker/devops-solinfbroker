apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-autenticacao.fullname" . }}-test-connection"
  labels:
    {{- include "api-autenticacao.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-autenticacao.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
