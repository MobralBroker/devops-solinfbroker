apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "database-postgresql-debezium.fullname" . }}-test-connection"
  labels:
    {{- include "database-postgresql-debezium.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "database-postgresql-debezium.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
